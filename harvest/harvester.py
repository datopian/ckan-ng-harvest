class HarvestSource:
    """ main harvester class to inherit """
    name = None  # name of the harvest source
    url = None  # url to harvest from
    config = None  # configuration (e.g validation_schema)
